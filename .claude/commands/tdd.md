---
name: tdd
description: Enter TDD mode — Red, Green, Refactor with explicit phase gates
---

Invoke the tdd skill now. For the current task: write the failing test first, confirm it fails for the right reason, then write minimum implementation to pass. Run the full test suite before declaring Green. Check coverage meets 80% minimum. Do not refactor until Green is confirmed.
