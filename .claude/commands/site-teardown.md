---
name: site-teardown
description: Fetch and analyze the full HTML, CSS, and JS of a reference site — not a summary, raw source
---

Invoke the site-teardown skill now. Fetch the target URL HTML. Parse all stylesheet and script hrefs. Fetch each CSS file as raw content — do not summarize. Identify named techniques for each key visual effect (glassmorphism, parallax, scroll animations, etc.) with exact CSS properties and values. Produce a teardown report with: page architecture, named effects, CSS/JS details, replication difficulty per effect, and what NOT to replicate. Flag any animated components as requiring manual review rather than screenshot-loop comparison.
